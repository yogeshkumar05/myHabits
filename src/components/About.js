import React from 'react';
import TopActionBar from './TopActionBar';

const About = (props) => {
    const {users} = props;    
    return(<div>
         <TopActionBar heading='About Page'/>
    </div>);
}
export default About;

