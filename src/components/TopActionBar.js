import React from 'react';

const TopActionBar = (props) => {  
    return(<div className='top-header-container'>
         <header className='header'>{props.heading}</header>
         <div className='page-actions'>pa...</div>
    </div>);
}
export default TopActionBar;

