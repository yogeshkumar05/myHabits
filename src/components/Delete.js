import React from 'react';
import TopActionBar from './TopActionBar';

const Delete = (props) => {
    const {users} = props;    
    return(<div>
        <TopActionBar heading='Delete Page'/>
    </div>);
}
export default Delete;

