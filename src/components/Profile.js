import React from 'react';
import TopActionBar from './TopActionBar';

const Profile = (props) => {
    const {users} = props;    
    return(<div>
        <TopActionBar heading='Profiiile Page'/>
    </div>);
}
export default Profile;

