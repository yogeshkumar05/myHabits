import React from 'react';
import TopActionBar from './TopActionBar';

const Update = (props) => {
    const {users} = props;    
    return(<div>
        <TopActionBar heading='Update Page'/>
    </div>);
}
export default Update;

