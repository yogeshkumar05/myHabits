import React from 'react';
import TopActionBar from './TopActionBar';

const Search = (props) => {
    const {users} = props;    
    return(<div>
        <TopActionBar heading='Search Page'/>
    </div>);
}
export default Search;

